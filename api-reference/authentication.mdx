---
title: "Authentication"
description: "Authentication in the app is required and it is done through a Bearer token"
---

To log in, you should use the Authentication token in the header.

```bash
'Authorization': 'Bearer <team_token>'
```